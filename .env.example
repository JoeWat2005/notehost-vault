# Template for ./.env. Copy to .env and fill in NOTES_TOKEN.
# NOTES_TOKEN comes from `provision-user.ps1 <username>` on the admin machine.

export NOTES_URL=https://notehost.net/process
export NOTES_TOKEN=REPLACE_WITH_YOUR_TOKEN
